services:
  - type: web
    name: mm-canvas-api
    runtime: python
    buildCommand: "apt-get install -y libjpeg-dev zlib1g-dev libpng-dev && pip install -r requirements.txt"
    startCommand: "gunicorn app:app --bind 0.0.0.0:$PORT"
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
